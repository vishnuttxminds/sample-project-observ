import { HighlightNameDirective } from './highlight-name.directive';

describe('HighlightNameDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightNameDirective();
    expect(directive).toBeTruthy();
  });
});
